import 'package:flutter/material.dart';

class RouteErrorView extends StatelessWidget {
  const RouteErrorView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Route Error View'),
      ),
    );
  }
}
